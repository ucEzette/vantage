[project]
name = "openclaw-vantage"
version = "0.1.0"
description = "Vantage Protocol skill for OpenClaw — turn your agent into a revenue-generating Vantage"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
